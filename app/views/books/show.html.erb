<% if flash[:notice] %>
    <div class="flash">
        <%= flash[:notice] %>
  </div>
<% end %>


<div class="container">
  <p id="notice"></p>
    <div class="row">
      <%= render 'users/list', user: @user, book: @book %>
      <div class="col-xs-9">
      <h2>Book detail</h2>
        <table class="table table-hover table-inverse">
          <thead>
           <tr>
           	 <th></th>
           	 <th></th>
           	 <th></th>
           	 <th></th>
           </tr>
          </thead>
           <tbody>
            <tr>
           	  <td><span>
                <%= link_to user_path(@user), class: "user_#{@user.id}" do %>
                  <%= attachment_image_tag @user, :image, :fill, 100, 100, fallback: 'no_image.jpeg', :size => '50x50' %>
                <% end %>
              </span></td>
           	  <td><span><%= link_to @bookn.title, book_path(@bookn.id), class: "book_#{@bookn.id}" %></span></td>
           	  <td><span><%= @bookn.body %></span></td>
              <% if @bookn.user_id == current_user.id %>
           	  <td><%= link_to "edit", edit_book_path(@bookn.id),class: "btn-sm btn-success edit_book_4353 edit_book_#{@bookn.id}" %></td>
              <td><%= link_to "destroy", book_path(@bookn.id), class: "btn-sm btn-danger destroy_book_#{@bookn.id}", method: :delete, "data-confirm" => "本当に消しますか?" %></td>
            </tr>
            <% end %>
           </tbody>
        </table>
<footer>
<div class="container">
　<div class="col-xs-3 col-xs-offset-9">
　 <p>CopyRight Infratop.inc</p>
</div>
</div>
</footer>