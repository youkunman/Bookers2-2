<div class="container">
　<p id="notice"></p>
　　　<div class="row">
		　　<%= render 'users/list', user: current_user %>
            <div class="col-xs-9">
             <h2>Users</h2>
              <table class="table table-hover table-inverse">
               <thead>
                <tr>
                 <th>image</th>
                 <th>name</th>
                 <th colspan="3"></th>
                </tr>
               </thead>
                <tbody>
                 <% @users.each do |user| %>
                  <tr>
                   <td><span><%= attachment_image_tag user, :image, :fill, 100, 100, fallback: 'no_image.jpeg', :size => '50x50' %></span></td>
                   <td><span><%= user.name %></span></td>
                   <td></td>
                   <td></td>
                   <td></td>
                   <td><%= link_to "show",user_path(user.id), class: "user_#{user.id}" %></td>
                  </tr>
                 <% end %>
                </tbody>
               </table>
            </div>
    </div>
</div>
<footer>
<div class="container">
　<div class="col-xs-3 col-xs-offset-9">
　 <p>CopyRight Infratop.inc</p>
</div>
</div>
</footer>